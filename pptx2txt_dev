#!/bin/bash
set -eu -o pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
readonly SCRIPT_DIR

docker container run \
  --name pptx2txt_dev$$ \
  -it \
  --rm \
  -u "$(id -u):$(id -g)" \
  -v "$PWD":/work \
  -v "$SCRIPT_DIR"/pptx2txt.py:/pptx2txt.py \
  shakiyam/pptx2txt "$@"
